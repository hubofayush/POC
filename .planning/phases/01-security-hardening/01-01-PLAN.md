# Phase 1 Plan 01-01: Security Hardening (Blockers)

## Goal
Remove the security blockers from the POC: replace mock auth with a real user
store + refresh rotation, enforce tenant isolation, CORS allowlist, fix LLM guard
API-key handling, stop leaking guardrail internals to clients, cap request bodies,
rate-limit auth, alarm on PHI detection failure, and make consent default-deny.

## Waves & Tasks

### Wave 0 — Prerequisites
- [x] 0.1 git init + purge secrets (commit `dadbfa9`)
- [ ] 0.2 GSD artifacts (STATE.md, CONTEXT.md, PLAN.md) → this file
- [ ] 0.3 Alembic baseline migration (audit_log + users + refresh_tokens)

### Wave 1 — Parallel workstreams
- [ ] 1.1 Auth rework: User/RefreshToken models, argon2, login w/ lockout,
      refresh rotation + reuse detection, logout, JWT iss/aud/jti, seed script
- [ ] 1.2 Org isolation: check_org wired into deps; org-scope audit/traces/invoke
- [ ] 1.3 CORS allowlist from settings
- [ ] 1.4 LLM guard: x-goog-api-key header, provider routing, JSON schema validation
- [ ] 1.5 Guardrail response sanitization (strip internal details)
- [ ] 1.6 Body-size limit middleware + auth rate limits
- [ ] 1.7 PHI fail-open + explicit alarm logging
- [ ] 1.8 Consent default-deny

### Wave 2 — Integration
- [ ] 2.1 Full test suite + smoke, fix regressions
- [ ] 2.2 Checkpoint report to user
- [ ] 2.3 SUMMARY.md + STATE.md + metadata commit

## Success Criteria
- All 8 wave-1 tasks done with atomic commits
- Full test suite green (updated for new auth/consent behavior)
- No secrets tracked; CORS/keys/limits env-driven
- Org isolation enforced on all data-bearing endpoints
