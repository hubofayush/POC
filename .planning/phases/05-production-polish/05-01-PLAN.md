# 05-01 PLAN — Production Polish & End-to-End Verification

Goal: close the project — make the deployment path real (secrets hygiene, deployment
runbook, in-container ops) and prove the full trust loop end-to-end with an executable
audit walkthrough (auth → guardrails → audit chain → traces → metrics).

## Waves

### Wave 0.1 — Plan artifacts (0.25h)
- Create 05-01-PLAN.md, update STATE.md (Phase 5 in progress)
- Commit: `docs(5-1): plan phase 5 production polish`

### Wave 1 — Secrets hygiene & container ops (0.5h)
- **1.1** `docker-compose.yml`: replace hardcoded `d5_secret` with `${POSTGRES_PASSWORD:?...}` interpolation (db + app DATABASE_URL) — fail-fast if unset; document in `.env.example` compose section
- **1.2** Dockerfile: `COPY scripts/ scripts/` so seeding/ops scripts exist in the image (alembic already runs on start)
- **1.3** Git secrets audit: confirm no keys/.env/*.pem tracked (already ignored — verify via `git ls-files`)

### Wave 2 — Deployment runbook (0.75h)
- **2.1** `docs/DEPLOYMENT.md`: host prep, prod `.env`, RSA key provisioning, `docker compose up -d --build` (auto-migrate via container CMD), verification, pg backup/restore, upgrade/rollback, TLS reverse-proxy note, hardening checklist
- **2.2** README: production section pointer to DEPLOYMENT.md

### Wave 3 — End-to-end audit walkthrough (1h)
- **3.1** `scripts/audit_walkthrough.py`: login (admin), run deterministic mixed invoke batch (clean passes + one block per guardrail layer), then cross-check:
  - `/audit/logs` contains an entry for every scenario with matching guardrail code
  - `/audit/verify` → `valid=true` (chain integrity)
  - `/traces` statuses match expected decision per scenario
  - `/metrics` guardrail decision counters incremented
  - exit non-zero on any mismatch

### Wave 4 — Verification & final checkpoint (0.5h)
- **4.1** Full gates: ruff + mypy + pytest (132+); restart dev server; walkthrough + smoke 13/13; `/audit/verify` valid live
- **4.2** STATE.md (Plan 05-01 COMPLETE — Phase 5 done) + 05-01-SUMMARY.md, metadata commit, final checkpoint report (project complete)

## Acceptance Criteria
- Compose yaml valid + no secrets hardcoded; no secrets tracked in git
- DEPLOYMENT.md complete; README updated
- `py scripts/audit_walkthrough.py` passes live (chain valid, traces/audit/metrics consistent)
- Gates green (ruff, mypy, pytest), smoke 13/13
