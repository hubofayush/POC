# 04-01 PLAN — Test Isolation, CI Gates & Documentation

Goal: close the production-readiness loop — lint/type/CI gates (Phase 0 items),
project docs and runbooks, Postman collection + .env sync. Test isolation was
already delivered in Phase 2 (d5_test.db).

## Waves

### Wave 0.1 — Plan artifacts (0.25h)
- Create 04-01-PLAN.md, update STATE.md (Phase 4 in progress)
- Commit: `docs(4-1): plan phase 4 quality gates and docs`

### Wave 1 — Static quality gates (1.5h)
- **1.1** CI workflow `.github/workflows/ci.yml`: on push/PR — ruff check, mypy, pytest (Python 3.12, dev extras); fail-fast
- **1.2** Ruff: add config to pyproject.toml (line-length 88, target py312), run `ruff check` on app/ + tests/, fix all findings
- **1.3** mypy: config in pyproject (ignore_missing_imports, python_version 3.12), run on app/ + tests/, fix what's cheap; document any deferred modules in the config exclusions

### Wave 2 — Dependency lock (0.25h)
- **2.1** `requirements.lock` from `pip freeze` (current working env) so CI and deploys install a known-good set; keep pyproject ranges as the floor
- **2.2** CI step installs from requirements.lock (pip, no resolver surprises)

### Wave 3 — Docs & runbooks (1h)
- **3.1** `README.md` (missing!): what it is, quickstart (env, keys, run, test), API overview, layout, prod notes
- **3.2** `docs/RUNBOOK.md`: setup, migrate, deploy (compose), upgrade path, backup/restore (pgdata), troubleshooting (port locks, stale dev DB, rate-limit 429s, circuit breaker)
- **3.3** `docs/OPERATIONS.md`: endpoints reference (/health*, /metrics, /audit/verify, /traces), metric catalog, log events, config reference (all settings)

### Wave 4 — Client artifacts (0.75h)
- **4.1** Postman collection: update auth (real login/refresh rotation), add /health/live, /health/ready, /metrics, /audit/verify, /traces; guardrail block payloads
- **4.2** `.env.example` sync: add TRUST_PROXY_COUNT, D3 block, AUTH rate limits, MAX_REQUEST_BODY_BYTES, GUARDRAIL_* set (grep shows most missing)

### Wave 5 — Verification & checkpoint (0.5h)
- **5.1** Full suite + ruff + mypy + live smoke; CI config review (no runner here — syntax sanity via yaml parse)
- **5.2** STATE.md + 04-01-SUMMARY.md, metadata commit, checkpoint report

## Acceptance Criteria
- `py -m pytest -q --no-header` green (132 + any new)
- `ruff check` clean on app/ + tests/; mypy clean per configured scope
- CI workflow yaml valid; README + runbook + ops doc written; Postman + .env.example current
- requirements.lock present; CI installs from it
